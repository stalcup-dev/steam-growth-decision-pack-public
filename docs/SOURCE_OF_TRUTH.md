# Source of Truth

This repo defines the public, reproducible pipeline for building a Steam discount engagement decision pack from the Mendeley dataset.

Authoritative definitions:
- Data contracts are defined in the user requirements.
- `src/` modules implement those contracts.
- `config.yaml` defines run-time defaults.
